services:
  redis:
    container_name: redis
    image: redis
    ports:
      - "6379:6379"
    networks:
      - app-net
    restart: always
    volumes:
      - redis-data:/data
volumes:
  redis-data:
